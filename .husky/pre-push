#!/usr/bin/env sh

echo "🛡️ Проверка перед пушем..."

# Только проверка типов во фронтенде (быстро и надёжно)
npm run typecheck --workspace=frontend || {
  echo "❌ Ошибка типов! Исправьте перед пушем."
  exit 1
}

echo "✅ Готово к пушу!"
